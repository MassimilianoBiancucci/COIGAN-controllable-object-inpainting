# @package _group_

dataset_name: defect_dataset_test

batch_size: 10
val_batch_size: 2
num_workers: 4

train:
  indir: ${location.data_root_dir}
  out_size: 256

  dataloader_kwargs:
    shuffle: True
    num_workers: ${data.num_workers}